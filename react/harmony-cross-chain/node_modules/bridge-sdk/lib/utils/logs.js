"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.logger = void 0;
const logFunc = ({ prefix = '', message }) => console.log(prefix, message);
let signale = {
    error: logFunc,
    start: logFunc,
    success: logFunc,
    await: logFunc,
    info: logFunc,
    pending: logFunc,
};
const context = this;
let logLevel = 0;
exports.logger = {
    error: (...args) => logLevel > 0 && signale.error.apply(context, args),
    start: (...args) => logLevel > 0 && signale.start.apply(context, args),
    success: (...args) => logLevel > 0 && signale.success.apply(context, args),
    wait: (...args) => logLevel > 1 && signale.await.apply(context, args),
    info: (...args) => logLevel > 1 && signale.info.apply(context, args),
    pending: (...args) => logLevel > 1 && signale.pending.apply(context, args),
    setLogLevel: (level) => (logLevel = level),
};
