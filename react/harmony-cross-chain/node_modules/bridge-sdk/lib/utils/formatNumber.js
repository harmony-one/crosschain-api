"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.divDecimals = exports.mulDecimals = void 0;
const bignumber_js_1 = require("bignumber.js");
const BN = require('bn.js');
exports.mulDecimals = (amount, decimals) => {
    const decimalsMul = `10${new Array(Number(decimals)).join('0')}`;
    const amountStr = new bignumber_js_1.BigNumber(amount).multipliedBy(decimalsMul);
    return new BN(amountStr.toFixed());
};
exports.divDecimals = (amount, decimals) => {
    const decimalsMul = `10${new Array(Number(decimals)).join('0')}`;
    const amountStr = new bignumber_js_1.BigNumber(amount).dividedBy(decimalsMul);
    return amountStr.toFixed();
};
