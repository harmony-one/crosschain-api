import { EXCHANGE_MODE, TOKEN } from './interfaces';
import { IHmyClient } from './blockchain/hmy';
import { IWeb3Client } from './blockchain';
import { ValidatorsAPI } from './api';
export declare const operation: (params: {
    api: ValidatorsAPI;
    web3Client: IWeb3Client;
    hmyClient: IHmyClient;
    token: TOKEN;
    type: EXCHANGE_MODE;
    amount: number;
    oneAddress: string;
    ethAddress: string;
    erc20Address?: string;
    maxWaitingTime: number;
}) => Promise<boolean>;
