import { Contract } from 'web3-eth-contract';
import Web3 from 'web3';
export interface IEthMethodsInitParams {
    web3: Web3;
    ethManagerContract: Contract;
    ethManagerAddress: string;
    ethTokenContract: Contract;
    gasPrice: number;
    gasLimit: number;
    gasApiKey: string;
}
export declare class EthMethods {
    private web3;
    private ethManagerContract;
    private ethTokenContract;
    private ethManagerAddress;
    private useMetamask;
    gasPrice: number;
    gasLimit: number;
    gasApiKey: string;
    constructor(params: IEthMethodsInitParams);
    setUseMetamask: (value: boolean) => boolean;
    approveEthManger: (amount: number, sendTxCallback?: (hash: string) => void) => Promise<any>;
    lockToken: (userAddr: string, amount: number, sendTxCallback?: (hash: string) => void) => Promise<any>;
    checkEthBalance: (addr: string) => Promise<any>;
}
