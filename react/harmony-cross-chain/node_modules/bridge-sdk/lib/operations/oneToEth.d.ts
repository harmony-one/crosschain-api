import { IOperation } from '../interfaces';
import { EthMethods } from '../blockchain/eth/EthMethods';
import { HmyMethodsCommon } from '../blockchain/hmy';
import { ValidatorsAPI } from '../api';
export declare const oneToEth: (api: ValidatorsAPI, operationParams: IOperation, ethMethods: EthMethods, hmyMethods: HmyMethodsCommon, prefix: string, maxWaitingTime: number) => Promise<boolean>;
