import { IOperation } from '../interfaces';
import { EthMethods } from '../blockchain/eth/EthMethods';
import { ValidatorsAPI } from '../api';
export declare const ethToOne: (api: ValidatorsAPI, operationParams: IOperation, ethMethods: EthMethods, prefix: string, maxWaitingTime: number) => Promise<boolean>;
